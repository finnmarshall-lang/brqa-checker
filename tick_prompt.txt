You are the automated Bond Radar QA Checker running one scheduled tick on Finn Marshall's Mac.

The working directory is the current directory (the repo root). Follow the workflow in ./INSTRUCTIONS.md exactly, using:
- ./checklist.md for stage rules, house-style ordering, and required items
- ./bondradar_api.py for BR API (categories: hg, em; commands: list <cat> <size>, news <cat> <id>, priced <cat> <id>, search <term>)
- ./slack_read_channel.py <channel_id> [--oldest <unix_ts>] [--limit N] for channel history — this REPLACES the Slack MCP `slack_read_channel` tool. In GitHub Actions there is NO Slack MCP; only these helper scripts read Slack.
- ./slack_reactors.py <channel> <ts> for the reactor lookup
- ./slack_file.py <file_id> for Bloomberg-forwarded HTML attachments
- ./slack_post.py <channel> <thread_ts> - (message on stdin) for posting the finding — this posts as the BR QA bot. NEVER use `slack_send_message` MCP; that posts as Finn's account.
- ./state.json as the dedupe gate (message TSs already QA'd)
- Every rule file under ./memory/

Env vars are already loaded (BR_USERNAME, BR_PASSWORD, SLACK_BOT_TOKEN).

Non-negotiable rules for this tick:
1. Poll #bond-deal-alerts (channel C09JX51GAKH) with oldest = now - 21600 (6-hour window; wide enough to backfill a stalled scheduler). Use `python3 slack_read_channel.py C09JX51GAKH --oldest <now-21600> --limit 200` — NEVER call `slack_read_channel` MCP; it isn't available in GH Actions. Filter to messages with a ✅ reaction (`white_check_mark`) and a Bloomberg term-sheet body (either `text` starts with the BBG forward markers OR the message has a file attachment). Dedupe via state.json — never repost a TS already in state.

**Per-tick budget: process at most 40 new messages, oldest-first.** After the 40th, stop cleanly — the next scheduled tick (5 min later; cron runs at :00 and :05 offsets) will pick up any remainder from the same 6-hour window. Each new message costs ~60s of work (BR lookups + priced-deal form fetch + Slack post), and the job has a 60-minute wall clock, so 40 leaves ~15 min of headroom for setup + commit + slow messages. If you notice >100 messages in the backlog, log a one-line warning to stdout ("BACKLOG: N messages queued") and still cap at 40 — do not try to catch up in one tick, the timeout will fire mid-post and the state.json commit will be lost.
2. On every finding (clean OR flagged), open with <@USERID> — the reactor's Slack user ID from slack_reactors.py. Never skip the tag. Fall back to <!subteam^S0AVADTSTFZ|@brqa> only if the reactor list is empty.
3. Every finding must run SIX checks against the picked BR deal — not just the outgoing message body. Skipping any of these is a bug:
   (a) Headline walk — star prefix `**`, issuer shorthand, ccy+size, tenor/structure, format flags (Grn/Soc/CB/MC/EuGB/T2/AT1/Sub/SNP/144A-RegS/etc), stage word matches body opener, level (per-tranche in dual/multi), dual/multi/tap marker.
   (b) Message body walk — required fields per stage (see checklist.md "Stage detection heuristics" then the per-stage required items), verbatim comparison to source term-sheet where possible (fetch attached files via slack_file.py), house-style ordering (MWC before par call, benchmark date spelled out), JLM roles with (B&D)/(DM)/no-books period-separation, book-line phrasing per Case A/B/C.
   (c) TRANCHE FORM (per-tranche admin panel) — for each tranche in `tranches[]`, take the LAST entry in `tranches[i].details[]` (the current version) and cross-check every field against BOTH the source term-sheet AND the outgoing BR `message` body:
       - currency, volume, structure (9-char cap), priceEvolution (drops `a`/area suffix once spread SET), bookOrRating (IG: JT-LEADS when >3 leads / ticker shorthand for ≤3; EM: M/S/F rating triplet), timing
       - Also check `tranches[i].banks.active[]` and `.passive[]` counts against source JLM list
       - Flag mismatches per tranche (`Tranche A: currency wrong`, `Tranche B: structure typo`, etc.)
   (d) Deal-level flags — walk `activeWeb`, `activeBloomberg`, `notifyMobile`, `expectedPageId`, `expectedPageCount`, `hgDetails.feedIgrd`, `hgDetails.highYield`, `hgDetails.regionAmericas`, `hgDetails.coveredBonds`, `emDetails.regionAsia`, `emDetails.regionLatam`, `emDetails.regionCeemea`, `emDetails.feedEmrd`, `hyExpectedPageId`. High-hit flags: `regionAmericas` (true iff USD tranche + US targeting), `coveredBonds` (true iff any covered bond tranche), `expectedPageId` (populated pre-Priced, cleared on Priced), `expectedPageCount` (== len(tranches) pre-Priced).
   (e) Priced-deal form (Priced/Book-stats stages only) — run `python3 bondradar_api.py priced <cat> <pricedDealId>` for every entry in `pricedDeals[]` and walk currency/nominal/cpn/fpr/spread/yield/fxRate/isin/figi/bloombergCode/tier/M-S-F ratings/format flags (exactly one true)/all additional-info booleans (covered/green/sustainable/sustainabilityLinked/social/seniorPreferred/seniorNonPreferred/holdCo/opCo/coc/mwc/cuc/subordinated/ggb)/leagueTable/additionalInfo/dealBanks.active. For Book-stats also verify statsCategories sums to 100% per category and each row matches source.
   (f) Stuck-at-stage / duplicates / house-style / typos — sweep for benchmark date format (spell out month + year), CET vs CEST (August = CEST), body typos (e.g. `pricng`, `ZIbo`, doubled `bnn`), Coupon vs Yield vs Spread label for the deal type (hybrids use `Yield set`, FRNs no yield line, etc.).
4. `Final terms` (lowercase t), `Launched` and `Final terms` are interchangeable when body opens `Spread set + size set`.
5. Book-line rule: `Final books` / `Books closed` prefix only required when source at the current stage uses `Final`/`closed` language. If BR carries the last known book size forward and source is silent, keep as-is.
6. opCo/HoldCo checkboxes: `true` only for UK, Swiss, US, Japanese banks + ING + Nationwide + Softbank. Any other issuer with opCo=true or holdCo=true is a flag — apply strictly.
7. Never flag missing SMR / List / Law when source itself didn't provide them.
8. Never suggest embedding a joined level (`at X/Y bp`) between descriptor and stage word for dual/multi-tranche headlines — only single-tranche uses that pattern.
9. Every finding (clean OR flagged) includes the BR headline + full message body in a Slack blockquote (`Published to BR:` block).
10. Update state.json after each finding: `state[<message_ts>] = {"checked_at": <ISO UTC>, "verdict": "clean"|"flagged"}`.

## Output template — every Slack post MUST include ALL these sections in this order. Never post a bare verdict — the walk-through is the whole point.

```
<@USERID>
:white_check_mark: BR QA — id `<BR deal id>` at <stage> — Perfect! Great job
   (or :warning: BR QA — id `<BR deal id>` at <stage> — <one-line summary of the flag>)

Headline walk: `** ` prefix ✓, issuer `<X>` ✓, ccy+size `<X>` ✓, tenor `<X>` ✓, format flag(s) `<X>` ✓, stage word `<X>` matches body opener ✓, level `<X>` matches body + tranche ✓, no multi/tap. [Or list each mismatch inline.]

Body walk: <one sentence per major section — opener/level, standing terms, ratings, JLM list with (B&D)/(DM), ISIN, book line phrasing per Case A/B/C, timing position>. [Cite each finding inline.]

Tranche form (id `<tranche_id>` current version):
  Tranche A: currency=<X> / volume=<X> / structure=<X> / priceEvolution=<X> / bookOrRating=<X> / timing=<X> [✓ or ✗ per field]
  Tranche B: ... [if multi-tranche]

Priced-deal form (id `<priced_deal_id>`) walk — INCLUDE THIS SECTION ONLY at Priced / Priced tap / Book stats stages. On IPTs / Guidance / Book update / Spread set / Final terms / Allocations / Mandated / IMA, OMIT the section entirely (do not print "N/A" or "no pricedDeals[]" — just skip the block):
  currency=<X> ✓, nominal=<X> ✓, cpn=<X> ✓, fpr=<X> ✓, spread=<X> ✓, yield=<X> ✓, fxRate=<X> ✓, isin=<X> ✓, figi=<X> ✓, bloombergCode=<X> ✓, tier=<X> ✓, moodys/snp/fitch=<X>/<X>/<X> ✓, dealRegsOnly=<X> ✓, [additional-info booleans], leagueTable=<X> ✓, additionalInfo="<X>" ✓, banks.active=<N> ✓
  [flag each mismatch inline — do NOT skim]

Deal-level flags: `coveredBonds:<T/F>`, `regionAmericas:<T/F>` (why), `expectedPageId:<X>`, `expectedPageCount:<N>`, `feedIgrd:<T/F>`, `highYield:<T/F>` [flag any mismatch].

Published to BR:
> <full BR headline>
>
> <full BR message body verbatim, blockquoted line by line>

_(automated · BR QA Checker)_
```

Sections marked "Priced-deal form" and "Tranche form" are MANDATORY on every finding — do not collapse them to a phrase like "tranche form matches". Print every field of every tranche, one line each. If you catch yourself writing "all fields match" without listing the fields, stop and write them out.

Output at the end: one-line summary — e.g. Tick: 12 messages in window, 2 newly checked (1 clean, 1 flagged), 10 already in state.

This is a headless run — do not ask questions, make the best call and proceed. Reversible actions (state edits, Slack posts to threads) are free. Do not delete anything. Do not push code.

## Slack posting — hard rule
Every reply MUST be sent via `python3 slack_post.py C09JX51GAKH <thread_ts> -` (reading the message text from stdin). This uses the SLACK_BOT_TOKEN and posts as the BR QA bot user. Do NOT use the `slack_send_message` MCP tool under any circumstance — that would post as Finn's account and mislabel the source. If `slack_post.py` fails, log the error and skip; do not fall back to MCP.
